# Epistemic Return: A Nearly Finished Argument Reopens

This synthetic example shows the central Fricturn transition.

```text
Q1 → S1 → I1 → A1 → new primary evidence E7
   → friction F3 → epistemic return R1 → Q2 → A2
```

Q1 assumed that a practice appeared only after a named media transition. S1 bounded the corpus, I1 interpreted the pattern, and A1 passed its argument gate. E7 is then verified as earlier primary evidence and changes the premise rather than merely adding another example.

Nothing is deleted. Q1, I1, and A1 remain in history. R1 records that the Argument gate is `INVALIDATED`, while the source-identity gate for unaffected S1 remains `VALID`. The researcher approves return from `ARGUMENT` to `QUESTION`. Q2 supersedes Q1 and weakens the thesis from historical origination to a bounded transformation claim. A2 is not treated as complete until affected gates run again.

See [return record](return-record.json) and [judgment record](judgment-record.json).
